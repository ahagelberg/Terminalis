<Window x:Class="TabbySSH.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Input" Height="150" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="PromptTextBlock" Text="Enter value:" Margin="0,0,0,10"/>

        <TextBox Grid.Row="1" x:Name="InputTextBox" Margin="0,0,0,15" 
                 KeyDown="InputTextBox_KeyDown"/>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="OK" x:Name="OkButton" 
                    Click="OkButton_Click" Padding="15,5" Margin="0,0,10,0" IsDefault="True"/>
            <Button Grid.Column="2" Content="Cancel" x:Name="CancelButton" 
                    Click="CancelButton_Click" Padding="15,5" IsCancel="True"/>
        </Grid>
    </Grid>
</Window>

