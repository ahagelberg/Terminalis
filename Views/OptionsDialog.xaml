<Window x:Class="TabbySSH.Views.OptionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Options" Height="600" Width="780"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource WindowBackground}">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" ResizeBorderThickness="4" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>
    <Grid Margin="0" Background="{DynamicResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left navigation -->
            <Border Grid.Column="0" Background="{DynamicResource PanelBackground}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,1,0">
                <ListBox x:Name="NavListBox" Background="Transparent" BorderThickness="0"
                         ItemContainerStyle="{StaticResource SettingsNavItemStyle}"
                         SelectionChanged="NavListBox_SelectionChanged"
                         SelectedIndex="0">
                    <ListBoxItem x:Name="NavStartupItem" Content="Startup"/>
                    <ListBoxItem x:Name="NavAppearanceItem" Content="Appearance"/>
                </ListBox>
            </Border>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="Transparent"/>

            <!-- Right content: single scrollable page with group titles -->
            <ScrollViewer x:Name="ContentScrollViewer" Grid.Column="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                          Margin="0" Padding="24,20" ScrollChanged="ContentScrollViewer_ScrollChanged" SizeChanged="ContentScrollViewer_SizeChanged">
                <StackPanel>
                    <!-- Startup -->
                    <Border x:Name="SectionStartup" Margin="0,0,0,24">
                        <StackPanel>
                            <TextBlock Text="Startup" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource WindowForeground}" Margin="0,0,0,16"/>
                            <CheckBox x:Name="RestoreSessionsCheckBox" Content="Restore active sessions on startup" Margin="0,8"/>
                        </StackPanel>
                    </Border>

                    <!-- Appearance -->
                    <Border x:Name="SectionAppearance" Margin="0,0,0,24">
                        <StackPanel>
                            <TextBlock Text="Appearance" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource WindowForeground}" Margin="0,0,0,16"/>
                            <Grid Margin="0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Theme:" VerticalAlignment="Center" Foreground="{DynamicResource WindowForeground}" Margin="0,0,16,0"/>
                                <ComboBox Grid.Column="1" x:Name="ThemeComboBox" SelectionChanged="ThemeComboBox_SelectionChanged"/>
                            </Grid>
                            <CheckBox x:Name="UseAccentColorForTitleBarCheckBox" Content="Use active session accent color for title bar" Margin="0,12,0,0"/>
                        </StackPanel>
                    </Border>
                    <!-- Extra space so last section can scroll to top and be selected (height set in code to viewport height) -->
                    <Border x:Name="BottomSpacer" Height="0"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- Footer buttons -->
        <Border Grid.Row="1" Background="{DynamicResource WindowBackground}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,1,0,0" Padding="24,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="1" Content="Save" x:Name="OKButton"
                        Click="OKButton_Click" Padding="20,8" Margin="0,0,10,0" IsDefault="True"
                        Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}"
                        BorderBrush="{DynamicResource ButtonBorder}"/>
                <Button Grid.Column="2" Content="Cancel" x:Name="CancelButton"
                        Click="CancelButton_Click" Padding="20,8" IsCancel="True"
                        Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}"
                        BorderBrush="{DynamicResource ButtonBorder}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
