<Window x:Class="TabbySSH.Views.OptionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Options" Height="600" Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource WindowBackground}">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" ResizeBorderThickness="4" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>
    <Grid Margin="15" Background="{DynamicResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Options" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="{DynamicResource WindowForeground}"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <GroupBox Header="Startup" Margin="0,0,0,10">
                    <StackPanel Margin="10">
                        <CheckBox x:Name="RestoreSessionsCheckBox" Content="Restore active sessions on startup" Margin="0,5"/>
                    </StackPanel>
                </GroupBox>
                <GroupBox Header="Appearance">
                    <StackPanel Margin="10">
                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Theme:" VerticalAlignment="Center"/>
                            <ComboBox Grid.Column="1" x:Name="ThemeComboBox" Margin="10,0,0,0" SelectionChanged="ThemeComboBox_SelectionChanged"/>
                        </Grid>
                        <CheckBox x:Name="UseAccentColorForTitleBarCheckBox" Content="Use active session accent color for title bar" Margin="0,10,0,0"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="Save" x:Name="OKButton" 
                    Click="OKButton_Click" Padding="15,5" Margin="0,0,10,0" IsDefault="True"
                    Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}"
                    BorderBrush="{DynamicResource ButtonBorder}"/>
            <Button Grid.Column="2" Content="Cancel" x:Name="CancelButton" 
                    Click="CancelButton_Click" Padding="15,5" IsCancel="True"
                    Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}"
                    BorderBrush="{DynamicResource ButtonBorder}"/>
        </Grid>
    </Grid>
</Window>

