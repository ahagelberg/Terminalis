<UserControl x:Class="TabbySSH.Views.DebugPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Debug Panel" FontWeight="Bold" FontSize="14" Margin="5" Padding="5"/>
        
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <StackPanel Margin="5">
                <!-- Active Modes Section -->
                <GroupBox Header="Active Modes" Margin="0,0,0,10">
                    <StackPanel Margin="5">
                        <CheckBox x:Name="ModeInAlternateScreen" Content="InAlternateScreen" Margin="0,2" Checked="ModeCheckBox_Changed" Unchecked="ModeCheckBox_Changed"/>
                        <CheckBox x:Name="ModeCursorKeyMode" Content="CursorKeyMode (DECCKM)" Margin="0,2" Checked="ModeCheckBox_Changed" Unchecked="ModeCheckBox_Changed"/>
                        <CheckBox x:Name="ModeInsertMode" Content="InsertMode (IRM)" Margin="0,2" Checked="ModeCheckBox_Changed" Unchecked="ModeCheckBox_Changed"/>
                        <CheckBox x:Name="ModeAutoWrapMode" Content="AutoWrapMode (DECAWM)" Margin="0,2" Checked="ModeCheckBox_Changed" Unchecked="ModeCheckBox_Changed"/>
                        <CheckBox x:Name="ModeCursorVisible" Content="CursorVisible (DECTCEM)" Margin="0,2" Checked="ModeCheckBox_Changed" Unchecked="ModeCheckBox_Changed"/>
                        <CheckBox x:Name="ModeBracketedPasteMode" Content="BracketedPasteMode" Margin="0,2" Checked="ModeCheckBox_Changed" Unchecked="ModeCheckBox_Changed"/>
                    </StackPanel>
                </GroupBox>
                
                <!-- Cursor Info Section -->
                <GroupBox Header="Cursor Information" Margin="0,0,0,10">
                    <StackPanel Margin="5">
                        <TextBlock x:Name="CursorInfoText" Text="Row: 0, Col: 0" Margin="0,2"/>
                        <TextBlock x:Name="BufferInfoText" Text="Buffer: main" Margin="0,2"/>
                        <TextBlock x:Name="BufferSizeText" Text="Buffer Size: 0 lines" Margin="0,2"/>
                    </StackPanel>
                </GroupBox>
                
                <!-- Scroll Region Section -->
                <GroupBox Header="Scroll Region" Margin="0,0,0,10">
                    <StackPanel Margin="5">
                        <TextBlock x:Name="ScrollRegionText" Text="Not active" Margin="0,2"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Top:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox x:Name="ScrollRegionTopInput" Grid.Column="1" Margin="0,2" TextChanged="ScrollRegionInput_TextChanged"/>
                            <TextBlock Grid.Column="2" Text="Bottom:" VerticalAlignment="Center" Margin="5,0,5,0"/>
                            <TextBox x:Name="ScrollRegionBottomInput" Grid.Column="3" Margin="0,2" TextChanged="ScrollRegionInput_TextChanged"/>
                            <Button Grid.Column="4" Content="Set" Margin="5,2,0,2" Padding="5,2" Click="SetScrollRegion_Click"/>
                        </Grid>
                    </StackPanel>
                </GroupBox>
                
                <!-- ANSI Code Testing Section -->
                <GroupBox Header="ANSI Code Testing" Margin="0,0,0,10">
                    <StackPanel Margin="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="AnsiCodeInput" Grid.Column="0" Margin="0,0,5,0" KeyDown="AnsiCodeInput_KeyDown"/>
                            <Button Grid.Column="1" Content="Send" Padding="5,2" Click="SendAnsiCode_Click"/>
                        </Grid>
                        <ComboBox x:Name="AnsiCodeExamples" Margin="0,5,0,0" SelectionChanged="AnsiCodeExamples_SelectionChanged">
                            <ComboBoxItem Content="-- Examples --" IsEnabled="False"/>
                            <ComboBoxItem Content="CUP [10;20H" Tag="\x1B[10;20H"/>
                            <ComboBoxItem Content="VPA [15d" Tag="\x1B[15d"/>
                            <ComboBoxItem Content="CHA [30G" Tag="\x1B[30G"/>
                            <ComboBoxItem Content="SGR [1;31m" Tag="\x1B[1;31m"/>
                            <ComboBoxItem Content="Erase Screen [2J" Tag="\x1B[2J"/>
                            <ComboBoxItem Content="Set Scroll Region [5;20r" Tag="\x1B[5;20r"/>
                        </ComboBox>
                    </StackPanel>
                </GroupBox>
                
                <!-- Command Log Section -->
                <GroupBox Header="Command Log" Margin="0,0,0,10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Button Grid.Row="0" Content="Clear Log" HorizontalAlignment="Right" Margin="0,0,0,5" Padding="5,2" Click="ClearLog_Click"/>
                        <ListBox x:Name="CommandLogList" Grid.Row="1" MinHeight="200" MaxHeight="400">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Timestamp}" FontSize="10" Foreground="Gray"/>
                                        <TextBlock Text="{Binding DisplayText}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </GroupBox>
                
                <!-- Performance Metrics Section -->
                <GroupBox Header="Performance Metrics" Margin="0,0,0,10">
                    <StackPanel Margin="5">
                        <Button Content="Reset Metrics" Click="ResetMetrics_Click" Margin="0,0,0,5"/>
                        <TextBox x:Name="PerformanceMetricsText" TextWrapping="Wrap" FontFamily="Consolas" FontSize="10" IsReadOnly="True" VerticalScrollBarVisibility="Auto" MinHeight="150" MaxHeight="300"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
